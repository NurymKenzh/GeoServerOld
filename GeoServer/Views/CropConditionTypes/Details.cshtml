@model GeoServer.Models.CropConditionType

@{
    ViewBag.Title = GeoServer.Resources.Interface.Details;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@GeoServer.Resources.Interface.Details</h2>

<div>
    <h4>@GeoServer.Resources.CropConditionTypeTexts.CropConditionType</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Max)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Max)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink(GeoServer.Resources.Interface.Edit, "Edit", new { id = Model.Id }) |
    @Html.ActionLink(GeoServer.Resources.Interface.BackToList, "Index")
</p>
